# Copy this file to secrets.toml and edit values as needed.
# secrets.toml is gitignored — never commit real secrets.

# Backend base URL. Defaults to http://localhost:8000 if not set here.
API_BASE_URL = "http://localhost:8000"
